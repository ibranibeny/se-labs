# --- Submit-an-asset configuration ---

# Public repo coordinates the client uses to build the prefilled GitHub issue
# "new issue" link. Safe to expose; defaults to ibranibeny/se-labs if unset.
NEXT_PUBLIC_GITHUB_OWNER=ibranibeny
NEXT_PUBLIC_GITHUB_REPO=se-labs
